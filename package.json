{
  "name": "inverter",
  "version": "1.0.0",
  "description": "变色方块",
  "author": ["yanhaijing"],
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "r.js -o name=index out=js/main.js baseUrl=./js paths.zepto=empty: paths.baiduTemplate=empty:",
    "build:weixin": "r.js -o name=weixin out=js/weixin-main.js baseUrl=./js paths.zepto=empty: paths.baiduTemplate=empty:"
  }
}
